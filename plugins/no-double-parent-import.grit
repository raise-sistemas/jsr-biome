`import $imports from $path` where {
    $path <: r".*\.\./\.\..*",
    register_diagnostic(
        span = $path,
        message = "Avoid imports that traverse two or more parent directories (../..)."
    )
}
